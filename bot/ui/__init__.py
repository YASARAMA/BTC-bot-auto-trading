"""Local web UI: a small HTTP server on 127.0.0.1 plus a single-page dashboard.

The desktop build (PyInstaller) starts this server and opens it in a native window
(pywebview) or, if that is unavailable, in the default browser."""
